No title specified

PLOTS:

PAGE 1: "Page"

    PLOT 1: "Plot"
        Y_AXIS_FORMAT: "%g"

        X_variable: "sys.exec.out.time"
            UNITS: "s"
            TIME_NAME: "sys.exec.out.time"
            LABEL: "sys.exec.out.time"

        Y_variable: "sensor.concentration_sensor.Cm_measured"
            UNITS: "--"
            LABEL: "sensor.concentration_sensor.Cm_measured"
            TIME_NAME: "sys.exec.out.time"

        Y_variable: "dyn.organs.Cm"
            UNITS: "--"
            LABEL: "dyn.organs.Cm"
            TIME_NAME: "sys.exec.out.time"

PAGE 2: "Page"

    PLOT 1: "Plot"
        Y_AXIS_FORMAT: "%g"

        X_variable: "sys.exec.out.time"
            UNITS: "s"
            TIME_NAME: "sys.exec.out.time"
            LABEL: "sys.exec.out.time"

        Y_variable: "control.concentration_control.error"
            UNITS: "--"
            LABEL: "control.concentration_control.error"
            TIME_NAME: "sys.exec.out.time"

    PLOT 2: "Plot"
        Y_AXIS_FORMAT: "%g"

        X_variable: "sys.exec.out.time"
            UNITS: "s"
            TIME_NAME: "sys.exec.out.time"
            LABEL: "sys.exec.out.time"

        Y_variable: "control.concentration_control.control_increment"
            UNITS: "--"
            LABEL: "control.concentration_control.control_increment"
            TIME_NAME: "sys.exec.out.time"

    PLOT 3: "Plot"
        Y_AXIS_FORMAT: "%g"

        X_variable: "sys.exec.out.time"
            UNITS: "s"
            TIME_NAME: "sys.exec.out.time"
            LABEL: "sys.exec.out.time"

        Y_variable: "control.concentration_control.control_command"
            UNITS: "--"
            LABEL: "control.concentration_control.control_command"
            TIME_NAME: "sys.exec.out.time"
